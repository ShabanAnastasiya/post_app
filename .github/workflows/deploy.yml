name: Advanced Supabase CI/CD

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  # --- ЗАДАЧА №1: Проверка качества кода ---
  lint-and-test:
    name:  Lint & Test Migrations
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli-action@v1

      # Этот шаг запускает локальный Supabase, чтобы проверить миграции
      - name: Test migrations on a clean local database
        run: |
          echo ">>> Starting local Supabase instance..."
          supabase start
          echo ">>> Testing migrations by resetting the database..."
          supabase db reset
          echo ">>> Migration test successful!"

  # --- ЗАДАЧА №2: Деплой (Запустится только если lint-and-test прошел успешно) ---
  deploy:
    name: Deploy to Supabase
    runs-on: ubuntu-latest
    # Зависимость: эта задача ждет, пока `lint-and-test` завершится успешно
    needs: [lint-and-test]

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.ACTION_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli-action@v1

      # --- ШАГ ДЕПЛОЯ В DEV ---
      - name: Deploy Database & Edge Functions to DEV
        if: github.ref == 'refs/heads/develop'
        run: |
          echo ">>> Linking to DEV project..."
          supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_ID_DEV }}
          echo ">>> Deploying database migrations..."
          supabase db push --password ${{ secrets.SUPABASE_DB_PASSWORD_DEV }}
          echo ">>> Deploying Edge Functions..."
          supabase functions deploy --project-ref ${{ secrets.SUPABASE_PROJECT_ID_DEV }}

      # --- ШАГ ДЕПЛОЯ В PROD ---
      - name: Deploy Database & Edge Functions to PROD
        if: github.ref == 'refs/heads/main'
        run: |
          echo ">>> Linking to PROD project..."
          supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_ID_PROD }}
          echo ">>> Deploying database migrations..."
          supabase db push --password ${{ secrets.SUPABASE_DB_PASSWORD_PROD }}
          echo ">>> Deploying Edge Functions..."
          supabase functions deploy --project-ref ${{ secrets.SUPABASE_PROJECT_ID_PROD }}

      # --- ШАГ УВЕДОМЛЕНИЯ В SLACK (Выполнится в любом случае после деплоя) ---
      - name: Send Slack Notification on Success
        if: success() # Выполнить только если предыдущие шаги были успешны
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          fields: repo,message,commit,author,action,eventName,ref,workflow # Какую информацию отправлять
          text: '✅ Deployment to `${{ github.ref_name }}` completed successfully!'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Send Slack Notification on Failure
        if: failure() # Выполнить только если что-то сломалось
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          fields: repo,message,commit,author,action,eventName,ref,workflow
          text: '🚨 Deployment to `${{ github.ref_name }}` FAILED! Check logs.'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}